<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.druvu</groupId>
		<artifactId>druvu-lib-json</artifactId>
		<version>1.0.0</version>
	</parent>

	<artifactId>druvu-lib-json-yaml</artifactId>

	<name>druvu-lib-json YAML Backend</name>
	<description>YAML 1.2 backend for the druvu-lib-json building and parsing API, on snakeyaml-engine</description>

	<dependencies>
		<dependency>
			<groupId>com.druvu</groupId>
			<artifactId>druvu-lib-json-api</artifactId>
		</dependency>
		<dependency>
			<groupId>com.druvu</groupId>
			<artifactId>druvu-lib-loader</artifactId>
		</dependency>
		<!-- The consumer brings its own snakeyaml-engine, exactly like the other backend modules. -->
		<dependency>
			<groupId>org.snakeyaml</groupId>
			<artifactId>snakeyaml-engine</artifactId>
			<scope>provided</scope>
		</dependency>
		<!-- TEST -->
		<dependency>
			<groupId>com.druvu</groupId>
			<artifactId>druvu-lib-json-api</artifactId>
			<type>test-jar</type>
		</dependency>
		<!-- A JSON backend alongside this one: the point of giving YAML its own front door is that
		     both can sit on one class path. TestFormatsCoexist proves it. -->
		<dependency>
			<groupId>com.druvu</groupId>
			<artifactId>druvu-lib-json-gson</artifactId>
			<version>${project.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.google.code.gson</groupId>
			<artifactId>gson</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testng</groupId>
			<artifactId>testng</artifactId>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
		</dependency>
	</dependencies>

</project>
